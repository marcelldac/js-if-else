// código 
semaforo = "amarelo"; //declaração de variável

if (semaforo == "verde"){ //condicional if (se o semaforo for verde imprima 'pode passar')

    console.log("Pode passar!");

} else if(semaforo == "vermelho") { //condicional else if (se o semaforo for vermelho imprima 'parar')

    console.log("Parar");

} else { //condicional else (se o sinal não for verde, nem vermelho, imprima 'atenção')

    console.log("Atenção!");
}
// código